def parenthese(n, paren_list=None)
    if paren_list is None:
        paren_list = ["("]

    if len(paren_list) == n * 2:
        return paren_list

    for i in :
        #odd
        if len(paren_list) % 2:
